---
name: AgentSkill-Akasha-KT
description: 阿卡西记录——从本地知识库中查找可复用经验，并在明确授权后记录、修正或归档正式知识。通过“帮我找下”“之前遇到过吗”“记一下”“修正这条记录”“离线保存这篇文章”等自然语言触发。
license: MIT
---

# Akasha-KT 阿卡西记录

## 1. 职责

本 Skill 只处理四类意图：

1. **查找**：从阿卡西已有记录中查找经验或知识。
2. **记录/归档**：整理原创经验、知识、创意，或完整离线保存公开文章。
3. **验证/修正**：核验已有记录，更新错误、过时或不完整内容。
4. **系统治理**：调整模板、流程、索引、标签或 Web 解析契约。

先识别意图，再读取对应 workflow。不要同时加载与当前意图无关的流程。

| 用户意图 | 必须读取 |
|---|---|
| 询问过去经验、找方案、“帮我找下”“之前遇到过吗” | [search.md](references/workflows/search.md) |
| “记一下”“归档”“离线保存这篇文章” | [record.md](references/workflows/record.md) |
| “验证下”“这条不对”“信息过期了” | [validate.md](references/workflows/validate.md) |
| 修改 Skill、模板、workflow、schema、索引或标签规则 | [governance.md](references/workflows/governance.md) |

## 2. 三层边界

所有路径都相对于本文件所在的 Skill 根目录。

| 层 | 路径 | 用途与权限 |
|---|---|---|
| 数据层 | `data/*.md`、`assets/<record-name>/*` | 正式知识与配套资源；是查询、引用和展示给用户的唯一知识来源。删除需用户确认。 |
| 索引层 | `references/INDEX.md`、`references/tag-registry.md` | 派生文件清单与标签注册；可维护，但不能作为知识查询结果。 |
| 系统层 | `SKILL.md`、`README.md`、`references/workflows/*`、`references/templates/*`、`references/scripts/*`、`references/reviews/*` | 流程、契约、工具说明和审核证据；仅在相应流程中读取，不能作为用户知识返回。 |

必须遵守：

- 本地知识检索与回答的白名单只有 `data/*.md`；即使系统层命中，也按“阿卡西本地未命中”处理。
- 不把 `references/INDEX.md` 全文作为检索语料发送给模型。
- 正式记录只写入扁平的 `data/*.md`；不得在 `data/` 下建子目录，也不得在其他位置创建知识记录。
- 资源放在 `assets/<record-name>/`，正文使用相对路径；非 `data/` 层历史资料可放在 `assets/archives/`。
- 系统规则只能通过治理流程修改，不得混入普通记录或修正任务。

## 3. 全局执行规则

1. **查询与写入分离**：历史经验查询可自动只读执行；记录、修正、删除、联网和 Git 发布分别服从当前用户授权，不能互相推导。
2. **先查当前文件，不凭记忆冒充库存**：查询必须实际检索 `data/*.md`；命中后检查状态、日期和适用版本。
3. **本地未命中先明说**：先回复“阿卡西本地未命中”。仅当用户明确要求继续调研，或原始意图本身包含外部调研时，才联网，并区分本地记录与外部结论。
4. **写入前先确认成品**：记录前完成重复、事实、时效、来源和敏感信息检查，向用户预览标题、概要、类型、来源、脱敏结论及目标文件；确认后才能写入正式库。
5. **正式记录不是聊天纪要**：原创内容应按可独立阅读的知识博客或技术/学术论文组织，交代问题、背景、证据、推理、方法、边界与结论；篇幅随主题调整，不为形式堆字数。
6. **不落草稿**：写入 `data/` 的必须是符合模板的正式候选，不得放占位符、临时片段或“稍后补充”的半成品。
7. **语义审核与机器检查都要做**：新增或实质改写正文必须通过无历史、只读的陌生读者冷读；结构和索引按 [scripts/README.md](references/scripts/README.md) 检查。两者不能互相替代。
8. **索引属于完成闭环**：新增或修改记录后，由脚本同步 `INDEX.md`；出现新标签时同步 `tag-registry.md`。
9. **发布只处理本次范围**：需要提交或推送时，先同步远端、精确核对本次文件，再按当前授权执行；禁止用宽泛暂存把无关改动带入提交。无法发布时如实说明，本地结果不能冒充已发布。

## 4. 记录契约速查

完整结构以 [record-template.md](references/templates/record-template.md) 为唯一权威。

- 文件名：自解释的 `kebab-case.md`。
- 标签：至少 1 个 `domain`，恰好 1 个 `type`；可加 `specialty`、`custom`。先查 [tag-registry.md](references/tag-registry.md)，优先复用。
- 必填元数据：标签、来源、收录日期、状态、可信度；外部来源还要记录来源日期。
- 必填正文：`### 概要`、与主题匹配的正文、`### 验证记录`。
- 相关记录：使用 `./other-record.md`；只能作为扩展阅读，正文自身必须成立。
- 图片：`../assets/<record-name>/01-description.png`；序号按正文出现顺序，alt 文本应可帮助无图阅读。

状态只能使用下列固定枚举：

| 状态 | 含义 |
|---|---|
| ✅ 已验证 | 经实践确认有效 |
| ⚠️ 待验证 | 理论可行但未实测 |
| 📘 有效 | 信息准确且当前适用 |
| 🔄 待更新 | 已知存在版本或内容变化 |
| 📕 已过时 | 不再适用当前环境 |
| ❌ 已废弃 | 已证伪或存在明确风险 |
| 🔬 实验性 | 非主流或试探性方案 |
| 💡 构想中 | 创意或灵感阶段 |

## 5. 共同完成边界

- **只读查询**：实际检索、检查命中记录、回答并标明来源边界后完成；不得顺手写库。
- **记录或修正**：只有正式候选、语义门禁、结构校验、索引同步和授权范围内的发布动作均已处理后，才能按真实状态报告结果。
- **离线归档**：除共同门禁外，还必须完整保存公开原文及内容资源；详见 `record.md` 的独立分支。
- **系统治理**：必须证明功能与契约未意外变化，并完成独立只读复审；不得用数据层记录承载系统规则。

## 6. 禁止的捷径

- 凭模型记忆回答“库里有没有”。
- 把外部搜索结果说成阿卡西已有记录。
- 把 `references/**` 命中当作知识命中。
- 未经确认写入、删除、联网、提交或推送。
- 手工维护派生文件清单来替代索引脚本。
- 用脚本退出码代替事实正确性、脱敏判断或陌生读者审核。
- 让参与撰写的上下文假装成无上下文读者，或用一句“已审阅”代替绑定候选摘要的完整证据。
