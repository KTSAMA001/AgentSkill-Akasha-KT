# 冷读审核：Unity BRG 逐株 Buffer 的 32 字节压缩与量化 ABI

- 目标记录：`data/unity-brg-packed-instance-buffer-quantization.md`
- 候选 SHA-256：`E074B73FD836C8440B87ECD215C05DF0FC3728D3CDA905A661A4486257F9576D`
- 直接引用资源：无
- 审核日期：2026-08-26
- 审核任务/上下文 ID：`/root/release_read_abi`
- 读者隔离：新建子 Agent，`fork_turns=none`，不继承父任务对话
- 输入白名单：目标记录的冻结审核副本、目标读者画像、审核问题；禁止其它输入
- 目标读者：了解 Unity、ComputeBuffer/HLSL 与 GPU Instancing，但不了解任何特定项目历史的工程师。
- 最终结论：可发布

## 完整审核提示

```text
你是独立的高级 Unity GPU 数据布局技术读者。只做只读审核，不修改任何文件，不读取 Git、源码、review 证据、其它知识记录或父任务上下文。

目标记录（仓库相对名）：data/unity-brg-packed-instance-buffer-quantization.md
唯一允许读取的审核输入副本：.codex-akasha-rewrite/unity-brg-packed-instance-buffer-quantization.md
预期 SHA-256：E074B73FD836C8440B87ECD215C05DF0FC3728D3CDA905A661A4486257F9576D
目标读者：了解 Unity、ComputeBuffer/HLSL 与 GPU Instancing，但不了解任何特定项目历史的工程师。

先计算输入副本 SHA-256；不匹配就停止并判定不可发布。只根据本文自身冷读。知识记录必须包含元数据以及“概要”“验证记录”，不能仅因这些必要结构存在而否定。

审核问题：
1. 用自己的话复述本文的独立主题、适用范围及明确不负责的内容，判断目标读者能否单篇理解。
2. 逐节判断是否服务于主题；扫描工作日志、交付报告、任务人物、项目私有背景、审查过程、提交号、本机路径、RunId、测试类名等越界内容。
3. 检查术语、32 B 布局、字节偏移、位序、量化/解码、Half 中点规则、CPU-HLSL 寻址、Buffer 容量、逻辑索引到物理地址、保守 Bounds、生命周期与发布接口是否内部一致。
4. 特别检查有符号字段、非负幅值字段、饱和字段、非法输入、保守风摆公式与“已覆盖/未覆盖”主张是否自洽，但不要预设结论。
5. 检查每项事实强度是否受文内证据和验证边界支撑；当前成立、历史证据、待验证是否清楚。
6. 评价标题、摘要、段落顺序、表格、公式和关联阅读的易读性；列出阻断项和非阻断建议。
7. 结论前再次计算 SHA-256，确认审核期间未变化。

必须完整输出以下标题：
## 审核前 SHA-256
## 独立主题复述
## 章节必要性与易读性
## 内部一致性与证据边界
## 隐私及工作日志扫描
## 阻断项
## 非阻断建议
## 审核后 SHA-256
## 最终结论

最终结论只能写“可发布”或“需修改”。
```

## 实际输入与起止摘要

- 实际输入：目标记录的冻结审核副本 `.codex-akasha-rewrite/unity-brg-packed-instance-buffer-quantization.md`
- 审核前 SHA-256：`E074B73FD836C8440B87ECD215C05DF0FC3728D3CDA905A661A4486257F9576D`
- 审核前资源摘要：无
- 审核后 SHA-256：`E074B73FD836C8440B87ECD215C05DF0FC3728D3CDA905A661A4486257F9576D`
- 审核后资源摘要：无

## 读者完整原始输出

```text
## 审核前 SHA-256

`E074B73FD836C8440B87ECD215C05DF0FC3728D3CDA905A661A4486257F9576D`

与预期一致。

## 独立主题复述

本文定义了一套面向受限 TRS 实例的 Unity BRG 32 B 逐株数据 ABI：将位置保留为 float32，旋转压缩为 smallest-three 10:10:10:2，连续标量使用指定 Half/UNorm16 规则，身份与地址保持整数语义；同时约束 CPU/HLSL 解码、BRG Metadata 寻址、Buffer 分配、量化后剔除及事务发布。

适用范围明确限定为有限、无剪切、非镜像、正统一缩放的数据模型，以及 Unity 2022.3 LTS、BRG/DOTS Instancing。本文明确不负责或尚未证明：

- 任意矩阵、非统一缩放、镜像和剪切的近似表达。
- 矩阵到规范四元数的完整跨语言提取协议。
- Quest Vulkan、Android Player、ConstantBuffer 后端闭环。
- 四个渲染 Pass 的同等级视觉等价。
- 设备帧时、功耗、物理显存或最终性能收益。
- 完整 Cell/Heap 编辑、卸载及作者工作流生命周期。

目标读者可以仅凭本文理解并实现 32 B ABI、主要寻址链和安全边界；关联记录是扩展阅读，不构成理解前提。

## 章节必要性与易读性

- 元数据、概要及第一节准确建立了状态、主题和证据边界。
- 第二至第四节依次给出输入契约、物理布局和编码规则，是 ABI 的核心。
- 第五、六节把字段布局连接到 BRG Metadata、GlobalSlot、共享表地址及总 Buffer 容量，顺序合理。
- 第七节解释“为什么 CPU 必须采用解码后真值”，避免文章退化为单纯节省字节的说明。
- 第八节虽超出纯位布局，但发布事务直接关系到 CPU/GPU 状态一致性，保留是必要的，且已明确不覆盖完整生命周期。
- 第九、十节将观察、限制和不可推出结论分开，显著降低性能误读风险。
- 检查清单和可复用结论适合工程落地。
- 表格、地址示例和公式整体易复算。唯一较明显的未定义实现词是“Prototype”，不影响主线理解。

## 内部一致性与证据边界

- 8 个 `uint`、各字段字节范围和 32 B stride完全闭合。
- 固定金样内部正确：
  - 三个位置字分别对应 `1/-2/3.5`。
  - identity 省略 `w`，三个零分量量化为 512，得到 `E0080200`。
  - Scale、UNorm 相位、风摆、负 Bounds、逆高度、Heap 和光照模式的高低位均与表格一致。
- Half 位算法与列出的边界向量自洽；特别是精确中点 `1.00048828125f` 按本文指定算法得到 `3C01`，与文中“不可替换为 ties-to-even”一致。
- 有符号 Bounds、非负风摆、严格正值 Scale/InverseHeight、随机相位饱和例外及非有限输入政策区分清楚。
- 当前最终生产者尚未独立拒绝负风摆、公共化帮助函数尚缺非有限门禁，均被明确写成待补闭环，没有伪装成已验证事实。
- Metadata 示例正确：`96 + 5×32 = 256`，两次 `uint4` 读取区间及重复追加 Slot 后得到 `416` 均一致。
- Buffer 布局公式正确包含 96 B 头部、空表哨兵和两次 16 B 对齐。两组样本的新旧差值均为 `8192×96 = 786432 B`。
- 风摆保守公式在文中列明的非负解码幅值、归一化方向、`heightMask∈[0,1]` 和幅值取最小值条件下成立；本文也明确禁止把该公式套用于有符号 Bounds 字段。
- 当前事实、历史证据和待验证边界总体区分良好。历史 Quest 记录明确不作为本 ABI 的设备证据。
- “四个 Pass 共用链”“逐 Heap payload 摘要”等可视为实现描述，但相应运行验证只覆盖有限 Forward 路径；文章后文已限制结论，没有据此宣称四 Pass 等价。

## 隐私及工作日志扫描

未发现任务人物、审查对话、交付报告、提交号、本机绝对路径、RunId、测试类名、账号或项目私有配置。

Unity 版本、图形后端、样本数量、验证日期及相对知识记录链接均属于理解证据边界所需信息，不属于越界工作日志。“Cell”“Heap”“StableGuid”已在正文定义，其出现服务于地址和生命周期边界。

## 阻断项

无。

该记录可以按当前“⚠️ 待验证”状态发布；不能在未补充设备及生产者门禁证据的情况下将状态提升为“已验证”。

## 非阻断建议

- 增加至少一组非对称旋转金样，覆盖 `a/b/c` 不同数值、四种省略索引、整体取反和最大分量并列。当前 identity 金样无法发现 `a/b/c` 互换，也不能区分多数 tie-break 实现。
- 为两个绝对 Buffer 样本补充 `C/S` 逻辑计数，使总字节数可独立复算；目前只能独立复算固定实例段差值。
- 明确最大风摆对 `-0` 的政策：允许保留 `0x8000`，还是规范化为 `0x0000`。
- 将随机相位非有限门禁和负风摆最终入口门禁同时加入采用检查清单。
- 为风场输入补充有限性、零方向处理及浮点误差余量条件；后续增加 CPU/HLSL 解码差异下的视锥边缘测试。
- 将“四 Pass 共用同一链”标为静态实现事实或设计要求，避免与尚未完成的四 Pass 运行等价验证混淆。
- 定义“Prototype”，并可将“概要”“验证记录”提升为二级标题，以改善文档层级。

## 审核后 SHA-256

`E074B73FD836C8440B87ECD215C05DF0FC3728D3CDA905A661A4486257F9576D`

与审核前及预期值一致，审核期间文件未变化。

## 最终结论

可发布
```

## 处理与复审

- 阻断项为无。本轮未修改候选，也未将任何建议描述为已实现。
- 保留“增加非对称旋转金样”：它提升跨实现测试对分量顺序、省略索引、符号与并列规则的辨别力；现有正文算法仍然明确，且候选已标为“待验证”，因此不改变当前结论的准确性。
- 保留“补充 `C/S` 逻辑计数”：它提升两个绝对 Buffer 数字的独立复算能力；正文已经明确这些绝对值依赖未展开计数，同时固定实例段差值可独立复算，因此没有形成错误主张。
- 保留“明确最大风摆 `-0` 政策”：这是位级规范化选择；`-0` 的幅值仍为零，不推翻当前非负幅值或保守 Bounds 结论。
- 保留“把随机相位非有限门禁和负风摆最终门禁加入检查清单”：它改善采用门禁的完整性；两项当前缺口已经在正文及验证边界中披露，候选没有声称闭环完成。
- 保留“补充风场有限性、零方向和浮点余量条件，并增加视锥边缘测试”：它强化严格数值证明与设备验证；正文已经列明公式成立的主要契约，并将目标设备和完整运行闭环保持为未验证。
- 保留“区分四 Pass 静态共链与运行等价验证”：它减少措辞误读；正文已明确四 Pass 等价尚未验证，并未用共链描述推出等价结论。
- 保留“定义 Prototype 并调整标题层级”：它只改善术语和文档导航，不影响 ABI、寻址、量化或证据边界的技术准确性。

## 发布前复算

- 复算执行者：`/root`
- 复算时间：`2026-08-26T08:58:38Z`
- 最终目标 SHA-256：`E074B73FD836C8440B87ECD215C05DF0FC3728D3CDA905A661A4486257F9576D`
- 最终直接引用资源：无
- 与本证据输入逐项匹配：是
