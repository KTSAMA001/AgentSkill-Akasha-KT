# 冷读审核：历史 Quest 3S 植被 BRG 与普通 GO 系统观察：性能边界与光照路径

- 目标记录：`data/quest-vegetation-brg-performance-lighting-validation.md`
- 候选 SHA-256：`BDBD60821A14AF9E8FF02EE53D07AC0B7EFF4EB60999F60154EEDCCE8509612C`
- 直接引用资源：无
- 审核日期：2026-08-26
- 审核任务/上下文 ID：`/root/qa_quest_260826_final`
- 读者隔离：新建子 Agent，`fork_turns=none`，不继承父任务对话
- 输入白名单：目标记录的冻结审核副本、目标读者画像、审核问题；禁止其它输入
- 目标读者：了解 Unity Profiler、Quest 性能预算与 A/B 测试，但不了解任何特定项目历史的工程师。
- 最终结论：可发布

## 完整审核提示

```text
你是独立的高级 Unity/Quest 性能评测技术读者。只做只读审核，不修改任何文件，不读取 Git、源码、review 证据、其它知识记录或父任务上下文。

目标记录（仓库相对名）：data/quest-vegetation-brg-performance-lighting-validation.md
唯一允许读取的审核输入副本：.codex-akasha-rewrite/quest-vegetation-brg-performance-lighting-validation.md
预期 SHA-256：BDBD60821A14AF9E8FF02EE53D07AC0B7EFF4EB60999F60154EEDCCE8509612C
目标读者：了解 Unity Profiler、Quest 性能预算与 A/B 测试，但不了解任何特定项目历史的工程师。

先计算输入副本 SHA-256；不匹配就停止并判定不可发布。只根据本文自身冷读。知识记录必须包含元数据以及“概要”“验证记录”，不能仅因这些必要结构存在而否定。

审核问题：
1. 用自己的话复述本文的独立主题、适用范围及明确不负责的内容，判断目标读者能否单篇理解。
2. 逐节判断是否服务于可复用的性能/光照验证方法与历史证据；扫描工作日志、交付报告、任务人物、项目私有背景、审查过程、提交号、本机路径、RunId、测试类名等越界内容。
3. 检查 A/B/A 方法、顺序平衡建议、变量控制、指标口径、原始数据、派生计算、统计解释、混杂因素、历史结果与当前结论的界线是否内部一致。
4. 检查是否把 Windows Editor、Quest、GPU/CPU、功能正确性、视觉等价、相关性与因果性混为一谈；判断所有数值结论是否有足够上下文且未过度外推。
5. 检查复测协议是否能把历史趋势升级为当前证据，并清楚区分完整系统比较与仅提交路径比较。
6. 评价标题、摘要、段落顺序、表格和关联阅读的易读性；列出阻断项和非阻断建议。
7. 结论前再次计算 SHA-256，确认审核期间未变化。

必须完整输出以下标题：
## 审核前 SHA-256
## 独立主题复述
## 章节必要性与易读性
## 内部一致性与证据边界
## 隐私及工作日志扫描
## 阻断项
## 非阻断建议
## 审核后 SHA-256
## 最终结论

最终结论只能写“可发布”或“需修改”。
```

## 实际输入与起止摘要

- 阅读前 SHA-256：`BDBD60821A14AF9E8FF02EE53D07AC0B7EFF4EB60999F60154EEDCCE8509612C`
- 阅读前直接引用资源摘要：无
- 结论前 SHA-256：`BDBD60821A14AF9E8FF02EE53D07AC0B7EFF4EB60999F60154EEDCCE8509612C`
- 结论前直接引用资源摘要：无

## 读者完整原始输出

```text
## 审核前 SHA-256

`BDBD60821A14AF9E8FF02EE53D07AC0B7EFF4EB60999F60154EEDCCE8509612C`

与预期值一致，继续审核。

## 独立主题复述

本文独立记录四组历史 Quest 3S 实机三段窗口，说明：

- BRG 与普通 GameObject 的完整系统路径在 CPU 侧压力上的历史差异。
- 可见几何、Alpha Clip、双面绘制和屏幕覆盖仍可能使两条路径共同受 GPU/Overdraw 限制。
- 两条路径使用不同的探针光照表达，因此实验不是只替换 Draw API 的微基准。
- 历史数据如何降级解读，以及如何通过同包、同进程、同输入、同可见工作量的复测升级为当前证据。

本文明确不负责证明当前 ABI、Shader、光照布局或运行时组织的 Quest 性能；不把跨 APK 几何实验当作严格单变量因果实验；不分解顶点、三角形、Alpha Clip 与片元成本各自占比；也不宣称两条光照路径视觉等价。

目标读者无需知道特定项目历史即可理解主要结论、数据限制和复测方法。`Heap`、`SceneAsset` 等定制术语均出现在必要的测试条件描述中，不妨碍单篇理解。

## 章节必要性与易读性

- 元数据与“概要”：必要。摘要同时给出核心趋势和最重要的会话、跨包及路径差异边界。
- “历史实验与当前实现的边界”：必要，防止历史 APK 结果被误套到当前实现。
- “四组 A/B/A 的直接结果”：核心证据。表格虽宽且信息密集，但指标、样本数、三段顺序和关键配置差异均可直接查阅。
- “测试对象、场地和相机参考尺度”：必要。它为密度、可见数、三角形规模、相机覆盖与光照条件提供数值上下文。
- “两种 Box 代理准确代表什么”：必要，是阻止把代理实验误写成“只改变顶点数”的关键章节。
- “如何解释 CPU、GPU、Overdraw 与内存”：必要，分别限定指标含义，没有把利用率、帧时、Stale 和 PSS 混为一类。
- “BRG、普通 GO 与 SH 的实际差异”：必要，说明这是完整系统比较且视觉、光照成本不严格等价。
- “当前实现与历史设备数据的边界”：与前文略有重复，但承担当前版本停止条件，保留合理。
- “当前实现的重测协议”：核心可复用方法，覆盖输入冻结、会话连续性、工作量、原始序列、统计口径和顺序平衡。
- “适用边界”：是有效的集中复盘，方便脱离正文引用。
- “相关记录”：标题和说明足以判断阅读方向，不是理解本文历史结论的前置依赖。
- “验证记录”：属于要求中的必要结构，内容简洁且只记录证据范围，不是工作日志。

标题准确但较长；段落顺序总体遵循“结论—数据—条件—解释—当前边界—复测”的合理路径。表格适合高级技术读者，但窄屏阅读体验一般。

## 内部一致性与证据边界

- 四组均是名义上的 BRG→GO→BRG 顺序窗口，但正文明确披露：只有 15% Box 保存同一 PID，其余会话连续性证据逐级减弱，且都没有自动切换日志。没有把四组写成同等强度证据。
- 所有历史组使用同一顺序、窗口较短、缺少逐样本序列和离散度，因此不能做独立显著性检验。正文对此有明确限制，也没有声称统计显著。
- A/B/A 回切用于观察恢复趋势；复测协议进一步要求跨会话重复并交替 A/B/A、B/A/B，方向正确。
- 历史构建内的模式切换与跨 APK 几何比较分开处理。31.111% 体积差异、采样时段及签名缺失均被保留为跨包混杂证据。
- `App Frame`、`CPU+GPU`、CPU/GPU 百分比、Stale 和 PSS 的口径均有解释。没有把 `CPU+GPU` 当纯 GPU Frame Time，也没有机械相加。
- PSS 的四个 MiB 派生差值按所述 KB 差值除以 1024 可复算；APK 体积差及比例也与表述一致。
- 高密度真实草的“GPU 是主要瓶颈”使用“强烈支持”而非绝对证明，并同时承认缺少独立 GPU Frame Time，结论强度基本匹配证据。
- Box 组的 CPU 解释限定为完整 GO 路径的总压力，未唯一归因于主线程、Renderer、LOD、探针或提交环节。
- Windows Editor 的 520 可见数被明确排除为设备三段证据；Quest 实机、Editor 默认姿态和当前实现没有混写。
- 功能正确性、视觉等价和性能没有混为一谈：历史比较被定义为光照与生命周期不同的完整系统比较；仅提交路径的复测则要求几何、光照、剔除、实例及图像误差门禁一致。
- 相关性与因果性界线明确。跨包只保留相关性；因果升级要求同包、同会话、预先声明变量和重复测量。
- 所有主要数值均附有设备、模型、实例数、相机或可见性上下文；没有外推到其它 Quest、其它模型或当前实现。

复测协议足以形成当前证据的主体闭环，并清楚区分“完整系统比较”和“仅提交路径比较”。不过，顺序随机化、时间序列相关性和 DVFS 处理仍可进一步操作化。

## 隐私及工作日志扫描

未发现阻断性越界内容：

- 无人员姓名、账号、组织或项目代号。
- 无提交号、本机绝对路径、仓库路径、RunId 或测试类名。
- 无交付报告、任务分工、审查过程或对话痕迹。
- `PID` 仅作为同进程连续性的实验字段，不是泄露性的运行标识。
- “人工观察者”“操作者切换说明”属于证据强度描述，不是任务人物记录。
- `SceneAsset`、`Heap`、`StaticLightColor` 等名称只用于定义被测系统，没有夹带理解本文所不需要的项目背景。
- 元数据、“相关记录”和“验证记录”均为知识记录的必要结构，不构成工作日志。

## 阻断项

无。

## 非阻断建议

1. 将元数据状态从泛化的“有效”细化为“历史证据有效；当前实现待复测”，可进一步避免状态被误读为当前性能已验证。
2. 将 APK 体积差明确表述为“未受控构建内容漂移的信号”，而不是暗示 APK 文件大小本身必然造成运行时性能差异。
3. 复测时除交替 A/B/A 与 B/A/B 外，可预先随机化成对会话的执行顺序，并明确以独立会话而非每秒样本作为主要统计重复单位，避免时间序列伪重复。
4. 明确 CPU/GPU 档位与频率是锁定变量、分层变量，还是完整系统的响应量；微基准宜锁定或分层，完整系统测试则可保留动态调频但需共同报告热状态。
5. 对完整系统比较也可增加“功能正确且达到各自预定视觉质量”的验收门槛；它不要求像素等价，但应避免用较低质量输出换取性能后直接比较优劣。
6. 可把宽表拆成“帧与利用率”和“Stale/PSS/会话条件”两表，或把 `CPU+GPU` 列名改为“VrApi CPU+GPU 字段”，进一步减少误读。

## 审核后 SHA-256

`BDBD60821A14AF9E8FF02EE53D07AC0B7EFF4EB60999F60154EEDCCE8509612C`

与审核前及预期值一致，审核期间输入未变化。

## 最终结论

可发布
```

## 处理与复审

- 阻断项：无。
- 非阻断建议 1：状态文案可进一步细化，但标题、概要、当前实现边界和适用边界已反复限定为历史证据；本轮保留该建议且不修改目标记录，不会削弱“历史证据有效、当前实现不可外推”的准确性。
- 非阻断建议 2：APK 体积差更适合作为未受控构建漂移的信号；现文已把它限定为跨包混杂并据此降低因果强度，而未断言文件大小直接导致性能差异，因此本轮保留该建议不影响结论准确性。
- 非阻断建议 3：随机化会话顺序及明确独立会话统计单位可增强后续复测；现协议已经要求跨会话多轮重复、交替顺序、置信区间和效应量，历史结果也未声称统计显著，因此本轮保留该建议不改变证据等级。
- 非阻断建议 4：进一步明确 DVFS 档位和频率的控制角色可提高复测可操作性；现文已经披露 15% Box 三段档位差异，并要求复测记录档位、频率、温度和电量，故本轮保留该建议不掩盖现有混杂。
- 非阻断建议 5：完整系统比较可补充各自视觉质量验收门槛；现文已经明确两条光照路径不严格等价，且没有用性能数据宣称某一系统在等价视觉质量下总体更优，因此本轮保留该建议不造成视觉等价或功能正确性的错误外推。
- 非阻断建议 6：拆分宽表或调整列名属于易读性改进；现文已经明确解释 `CPU+GPU` 的字段边界并禁止机械相加，故本轮保留该建议不影响指标解释。
- 以上非阻断建议均未在本轮实现，也不改变最终结论“可发布”。

## 发布前复算

- 复算执行者：`/root`
- 复算时间：`2026-08-26T08:58:38Z`
- 最终目标 SHA-256：`BDBD60821A14AF9E8FF02EE53D07AC0B7EFF4EB60999F60154EEDCCE8509612C`
- 最终直接引用资源：无
- 与本证据输入逐项匹配：是
