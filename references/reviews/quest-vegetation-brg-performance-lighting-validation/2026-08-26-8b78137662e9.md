# 冷读审核：Quest 植被 BRG 性能与静态光照验证

- 目标记录：`data/quest-vegetation-brg-performance-lighting-validation.md`
- 候选 SHA-256：`8B78137662E91A3D4E8B66E109534A1733FDBACA7164689AB70329EB8572B64B`
- 直接引用资源：无
- 审核日期：2026-08-26
- 审核任务/上下文 ID：`/root/cold_read_quest_sanitized`
- 读者隔离：新建子 Agent，`fork_turns=none`，不继承父任务对话
- 输入白名单：目标记录、目标读者画像、审核问题；资源为无
- 目标读者：第一次接触该系统、需要判断设备 A/B 证据强度的高级 Meta Quest/Unity 性能与视觉验证工程师
- 最终结论：可发布

## 完整审核提示

```text
你是一名高级 Meta Quest/Unity 性能与视觉验证工程师兼无历史冷读者。通过系统提供的 Skills catalog 解析 `AgentSkill-Akasha-KT` 根目录，进入该根后，只读相对路径 `data/quest-vegetation-brg-performance-lighting-validation.md`；不得在输出中打印物理绝对路径。预期 SHA-256 为 `8B78137662E91A3D4E8B66E109534A1733FDBACA7164689AB70329EB8572B64B`。允许输入只有本提示和该目标记录；禁止读取 Git、源码、其它记录、INDEX、workflow、审核证据、对话、资产或外部资料，禁止修改文件。阅读前与结论前各独立计算一次 SHA；不匹配或前后变化只能给“无法判断”。以第一次接触系统的读者视角完整复述并审核：四轮同会话 A/B/A、Session/PID、相机/可见性/设备状态门禁；504、0.03224994、±0.30、514；草地→大Box→15%；独立 APK 31.111%；静态模式 XZ 0.9105374 对 1.01428664；BRG/GO 光照与截图/Query 边界；流程图、表格、链接文字、证据强度、因果措辞、过度承诺与敏感信息。固定输出：阅读前 SHA-256；结论前 SHA-256；系统复述；敏感信息审查；发布阻断项（无则写无）；非阻断建议（无则写无）；最终结论：可发布|修改后发布|无法判断。只有零阻断且双 SHA 匹配才可发布。
```

## 实际输入与起止摘要

- 阅读前目标 SHA-256：`8B78137662E91A3D4E8B66E109534A1733FDBACA7164689AB70329EB8572B64B`
- 结论前目标 SHA-256：`8B78137662E91A3D4E8B66E109534A1733FDBACA7164689AB70329EB8572B64B`
- 阅读前资源摘要：无
- 结论前资源摘要：无

## 读者完整原始输出

阅读前 SHA-256：`8B78137662E91A3D4E8B66E109534A1733FDBACA7164689AB70329EB8572B64B`

结论前 SHA-256：`8B78137662E91A3D4E8B66E109534A1733FDBACA7164689AB70329EB8572B64B`

系统复述：

- 记录保存的是 2026-08-19 四组 Quest 3S 测试，每组均按“初始 BRG → 普通 GO → 回切 BRG”采样，但证据等级不同：

  - 稀疏真实草只有顺序窗口、状态截图和用户切换说明，没有明确同会话字段或 PID；设备实际可见数未保存，`520` 只是编辑器默认姿态数据。
  - 高密度真实草与大尺寸 Box 由报告声明同一应用会话并保存连续窗口，但没有 PID。
  - 只有 15% Box 保存三段同一目标 PID。
  - 四组都没有自动模式切换事件日志，头显姿态和预热时长也未程序化锁定。

- 高密度真实草为 8192 株、单株 640 顶点/704 三角形，三段均有机器诊断 `8192/8192` 可见，约 20 FPS、GPU 约 99%。这强烈支持该条件下 GPU 工作量为主要瓶颈，但无法把成本进一步唯一拆分到顶点、三角形、双面、Alpha Clip 或 Overdraw。
- 稀疏组的生成规则是名义 `504 × 504` 米，密度 `8192 / (504 × 504) ≈ 0.03224994` 株/平方米；实例中心另有最多 `±0.30` 米抖动，实际中心范围约 `504.59 × 504.60` 米，地面边长为 `514` 米。记录明确区分名义网格、实际中心范围、地面尺寸以及设备可见数缺失。
- “真实草 → 大尺寸 Box → 15% Box”不是单进程单变量实验：

  - 真实草换大 Box 同时改变拓扑、轮廓、Alpha Clip 覆盖和内部 Overdraw。
  - 大 Box 与 15% Box 虽保留了更多条件，但仍是不同 APK、不同采样时段。
  - 两个 Box APK 分别为 `270621866` 与 `186428728` 字节，差 `84193138` 字节，即 `31.111%`，且原因未解释。因此跨包结果只能作为相关性证据。
  - 各包内部 A/B/A 的回切恢复仍支持普通 GO 完整对象路径具有更高 CPU 侧总压力，但不能唯一归因到 Transform、Renderer、LOD、探针绑定或 Draw 提交中的某一项。

- 15% Box 的最大旋转无关 XZ 外接圆直径 `0.9105374` 米，小于最小中心距 `1.01428664` 米，因此静态 Mesh 在任意 Y 轴旋转下水平包络不相交。记录正确限定此结论不涵盖 Shader 风摆或其他运行时顶点位移。
- Box 两组“8192 个实例全部入画”来自头显观察和设备截图，不等价于程序化 `8192/8192` 可见计数。相机、FOV、Near/Far、刷新率、Eye Buffer、质量档、Heap 范围及设备档位能提供的条件均有列出；缺失项也逐项声明。15% Box 普通 GO 段获得更高 GPU 档位和时钟，记录没有忽略这一混杂条件。
- 四组比较的是完整系统路径：

  - BRG 使用逐株静态最终颜色。
  - 普通 GO 使用 GameObject、Transform、MeshRenderer、LODGroup、GPU Instancing、Unity BlendProbes 与 Bakery SH 运行时路径。
  - 因此它不是只替换 Draw API 的微基准，也不是严格光照等价对照。

- 当前 v4 的 `StaticLightColor` 8 B、`StaticBakerySh` 20 B 与每株固定约 128 B 数据布局没有通过这四组历史 APK 在 Quest 上复测；历史大 Box 明确是旧的 12 B/株 Root RGB。2026-08-25/26 的 Editor 测试只能证明其覆盖的数据与生命周期流程，不证明设备 FPS、GPU、PSS、热状态或 Shader 变体。
- 物理 QueryWorld 两级 BVH 增量与历史渲染设备证据被明确分开；碰撞代理使用独立 ColliderStreamer、物理半径和状态机，不直接读取渲染掩码。
- 数值内在关系可复算：密度、APK 差值、PSS 的 KB→MiB 差值、`8192 × 704 ≈ 576.7 万` 三角形以及旧格式 `98304 / 8192 = 12 B/株` 均一致。
- 两张表的单位、三段排列顺序和缺失证据说明清楚。正文的箭头流程链能表达 Buffer、ActiveHeapMask、Count/Scatter 和 Cell 卸载关系；它不是正式流程图，但不影响理解。
- 相关记录链接均使用有意义的标题和关系说明，没有“点击这里”式模糊文字，也没有暴露绝对路径。正文未提供原始报告或截图的可访问链接，但已明确其匿名化与不可仅凭本记录重装复现的限制。
- 因果措辞总体克制：使用“支持”“一致”“相关性证据”“不能唯一归因”“不是严格单变量证明”等限定，没有把截图当机器计数、把 Editor 测试当 Quest 证据，或把跨 APK 相关性包装成单变量因果。

敏感信息审查：

未发现账号、Token、人员身份、私有 APK 名、RunId、提交号、工程绝对路径或机器专属路径。记录包含精确性能基线、APK 字节数及内部运行时架构细节，属于可能具有项目商业敏感性的技术信息；是否允许面向组织外公开仍应服从发布方既有保密分级，但记录本身没有泄露可直接识别的凭据或私有定位信息。

发布阻断项：

无。

非阻断建议：

- 标题或顶部状态可增加“2026-08-19 历史 APK 基线”，降低只扫元数据的读者将其误认为当前 v4 实机基线的概率。
- 若发布策略允许，可补充匿名、内容寻址的采样报告或截图证据引用，增强第三方可追溯性；当前缺失已经被诚实披露，不构成逻辑缺口。
- 可把“数据组成”和“运行时执行链”拆成两个小图，避免现有单条箭头链被误读为所有存储项都按顺序转换。

最终结论：可发布

## 处理与复审

- 本轮以仓库相对路径和规范 Git blob 字节冻结候选，审核提示及原始输出均未记录本机物理路径。
- 发布阻断项为“无”，最终结论为“可发布”；历史 APK 与当前 v4 数据布局的边界已在正文明确披露。

## 发布前复算

- 复算执行者：主执行者 `/root`
- 复算时间：2026-08-26T02:18:48.1579899+08:00
- 最终目标 SHA-256：`8B78137662E91A3D4E8B66E109534A1733FDBACA7164689AB70329EB8572B64B`
- 最终直接引用资源：无
- 与本证据输入逐项匹配：是
