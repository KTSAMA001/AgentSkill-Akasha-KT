# Unity 植被 Painter 作者事务记录独立冷读审核证据

## 审核元数据

- 目标记录：`data/unity-vegetation-painter-authoring-transaction-workflow.md`
- 候选 SHA-256：`063EFD5798A41DDDBACEA8D9A8B827DCA0A11DF85EECA6A61874DA0130CCEDFB`
- 审核日期：`2026-08-26`
- 任务 ID：`/root/qa_painter_260826_final`
- 隔离方式：`fork_turns=none`
- 输入白名单：`.codex-akasha-rewrite/unity-vegetation-painter-authoring-transaction-workflow.md`
- 目标读者：了解 Unity Editor、SceneView、ScriptableObject 与 Undo，但不了解任何特定项目历史的工程师。
- 阅读前 SHA-256：`063EFD5798A41DDDBACEA8D9A8B827DCA0A11DF85EECA6A61874DA0130CCEDFB`
- 结论前 SHA-256：`063EFD5798A41DDDBACEA8D9A8B827DCA0A11DF85EECA6A61874DA0130CCEDFB`
- 发布阻断项：无
- 最终结论：可发布

## 初始审核提示（逐字）

~~~~text
你是独立的高级 Unity 编辑器工具与数据事务技术读者。只做只读审核，不修改任何文件，不读取 Git、源码、review 证据、其它知识记录或父任务上下文。

目标记录（仓库相对名）：data/unity-vegetation-painter-authoring-transaction-workflow.md
唯一允许读取的审核输入副本：.codex-akasha-rewrite/unity-vegetation-painter-authoring-transaction-workflow.md
预期 SHA-256：063EFD5798A41DDDBACEA8D9A8B827DCA0A11DF85EECA6A61874DA0130CCEDFB
目标读者：了解 Unity Editor、SceneView、ScriptableObject 与 Undo，但不了解任何特定项目历史的工程师。

先计算输入副本 SHA-256；不匹配就停止并判定不可发布。只根据本文自身冷读。知识记录必须包含元数据以及“概要”“验证记录”，不能仅因这些必要结构存在而否定。

审核问题：
1. 用自己的话复述本文的独立主题、适用范围及明确不负责的内容，判断目标读者能否单篇理解。
2. 逐节判断是否服务于 Painter 作者事务与 UI 交互主题；扫描工作日志、交付报告、任务人物、项目私有背景、审查过程、提交号、本机路径、RunId、测试类名等越界内容。
3. 检查 Authority→Stage→Preview/Feedback→Commit→Compile→Save→PublishNotification 的状态与失败边界，五种模式的交互、取消、Undo、StableGuid、跨 Heap 移动和多运行时 Cell 作者隔离是否内部一致。
4. 检查内存作者数据、派生缓存、磁盘保存、BRG 刷新与跨文件发布是否被错误合并成同一原子事务；SceneRevision、脏标记和恢复语义是否足以避免误用。
5. 检查 UI 文案承诺与实际反馈语义是否匹配，并判断当前成立、目标契约和未验证边界是否清楚。
6. 评价标题、摘要、术语、模式表、状态机、失败矩阵和关联阅读的易读性；列出阻断项和非阻断建议。
7. 结论前再次计算 SHA-256，确认审核期间未变化。

必须完整输出以下标题：
## 审核前 SHA-256
## 独立主题复述
## 章节必要性与易读性
## 内部一致性与证据边界
## 隐私及工作日志扫描
## 阻断项
## 非阻断建议
## 审核后 SHA-256
## 最终结论

最终结论只能写“可发布”或“需修改”。
~~~~

## 原始审核输出（逐字）

~~~~markdown
## 审核前 SHA-256

`063EFD5798A41DDDBACEA8D9A8B827DCA0A11DF85EECA6A61874DA0130CCEDFB`

与预期值一致。

## 独立主题复述

本文独立描述 Unity 植被 Painter 的作者事务：如何选定唯一写入 Authority，将五种编辑操作暂存在 Stage 中，提供符合模式语义的反馈，再依次完成作者数据 Commit、派生缓存 Compile、磁盘 Save 和渲染变更通知。

范围包括 Authority、多运行时 Cell 隔离、取消与 Undo、StableGuid、跨 Heap 移动、预览生命周期及失败恢复。明确不负责 Prototype 文件发布、BRG 内部地址和资源生命周期实现、物理、静态光照细节，以及跨多文件原子发布。

目标读者可以单篇理解核心状态机和责任边界；关联记录属于下游实现细节，不构成理解本文的前置条件。

## 章节必要性与易读性

- 元数据、概要和验证记录均为知识记录必要结构，内容与正文相符。
- 第一至三节建立对象、术语和 Authority 规则，是后续事务设计的必要前提。
- 第四、五节用模式表和状态机解释交互、反馈、提交与取消，信息互补且没有重复堆叠。
- 第六至八节处理事务分层、失败恢复、StableGuid、跨 Heap、Undo 与预览，是主题的技术主体。
- 第九节给出长操作和交互预算，服务于 Painter 可用性，没有扩展成一般性能文章。
- 第十节把主张映射到验证方法和失败信号，适合作为采用检查表。
- 关联阅读只承接本文明确排除的渲染、光照和物理细节，边界合理。

标题准确，概要能快速建立主链路；模式表、状态表和失败矩阵可交叉阅读，整体易读。

## 内部一致性与证据边界

- `Authority → Stage → Preview/Feedback → Commit → Compile → Save → PublishNotification` 在概要、状态机、状态表和失败矩阵中保持一致。
- Paint/Erase 以鼠标抬起提交；Replace/Reproject/Transform 以显式应用提交。五种模式的 Stage 内容、反馈承诺、取消方式和 Undo 边界相互兼容。
- 只有 Transform 承诺真实结果预览，其余模式只表达影响范围或目标集合；这与正文对 UI 文案的约束一致。
- StableGuid 在 Replace、Reproject、Transform、同资产跨 Heap 和 Undo 恢复中保持不变；RuntimeIndex、数组下标和 GPU Slot 均被明确降级为派生地址。
- 跨 Heap 移动同时处理源、目标 Heap，并明确禁止搬运旧 RuntimeIndex 编址的派生光照数据。
- 多运行时 Cell 通过唯一 Authority、显式选择、Scene 卸载隔离和 Undo 后按受影响范围刷新来避免串写。
- 作者内存、派生缓存、磁盘状态、正式渲染和跨文件发布没有被合并为同一原子事务。Save、通知和 BRG 刷新失败均有独立恢复依据。
- `SceneRevision + SessionGeneration + StableGuid` 的组合以及 Undo/Redo 后无条件清除 Stage，足以表达主要失效原则；SceneRevision 的具体递增规则仍可进一步明确。
- Dirty、Save 失败和 Undo 后磁盘状态的区别写得清楚，没有以画面一致冒充持久化成功。
- 本文呈现的是待验证的目标契约，不是具体工具的已验收报告。未验证范围已列出；“验证记录”主要描述可验证性，不能被解读为测试已经通过。

## 隐私及工作日志扫描

未发现工作日志、交付报告、任务人物、项目私有背景、审查过程、提交号、本机路径、RunId 或测试类名。

日期、来源、状态、可信度、验证记录及关联阅读均属于必要知识元数据或正文结构，不构成越界内容。

## 阻断项

无。

## 非阻断建议

1. 明确 SceneRevision 在每次成功作者 Commit 及其它合法作者状态变更时如何更新，并说明该更新与 Undo Group 的关系。
2. 明确 Domain Reload、进入播放模式及非正常 Scene 卸载时，Stage、选择缓存、预览补丁和 SessionGeneration 的清除或重建规则。
3. 验证记录中的 `SceneGuid` 与正文的 `SceneAssetID` 用词不一致，建议统一或说明两者差异。
4. 首次使用时写为 `BatchRendererGroup（BRG）`，并简短定义 PrototypeId、SceneAssetID。
5. 将通知条件进一步统一为明确的保存政策，并给出“已应用、待编译”“已保存、渲染刷新失败”等状态文案，减少“应用”和“已持久化”被混淆的风险。
6. 补充派生缓存发布规则：应用前复核作者内容键、不生成额外作者 Undo；若缓存被序列化，说明其 Dirty 处理。
7. 将验证记录区分为“已完成观察”和“待执行验证”；当前没有实际通过结果时，可明确写为尚未执行。

## 审核后 SHA-256

`063EFD5798A41DDDBACEA8D9A8B827DCA0A11DF85EECA6A61874DA0130CCEDFB`

与审核前及预期值一致，审核期间输入未变化。

## 最终结论

可发布
~~~~

## 处理与复审

### 发布阻断项

无。

### 非阻断建议复审

1. **SceneRevision 更新规则**：正文已经用 `SceneRevision + SessionGeneration + StableGuid` 建立复合失效保护，并明确 SessionGeneration 在关键事件中递增。补充 SceneRevision 的精确更新时机可提高实现一致性，但不改变本文现有事务边界，也不影响本版准确发布。
2. **生命周期切换规则**：正文已规定 Scene 卸载的隔离原则、播放模式默认只读以及会话资源释放责任。进一步列明 Domain Reload、播放模式切换和非正常卸载时各类临时状态的处置属于实现细化，不构成现有结论错误，因此不影响本版准确发布。
3. **SceneGuid 与 SceneAssetID 用词**：不一致仅出现在未验证边界的列举中，正文关于跨资产身份的正式规则始终使用 `SceneAssetID + StableGuid`。该术语修订能减少歧义，但不会改变核心身份契约，因此不影响本版准确发布。
4. **缩写及身份术语首次定义**：正文适用版本已经写出 BatchRendererGroup，且对象表和术语节足以建立核心关系。补充缩写及 ID 定义只改善首次阅读体验，不改变技术含义，因此不影响本版准确发布。
5. **保存政策和状态文案**：状态机、检查点表及失败矩阵已经区分 Commit、Compile、Save 和 PublishNotification，并明确禁止在保存失败时宣称已持久化。增加更细的 UI 状态文案是产品表达优化，不影响本版对反馈语义的准确约束。
6. **派生缓存应用细节**：正文已经要求缓存携带作者内容键和外部依赖版本，且禁止把部分缓存伪装为完整有效。补充 Undo、Dirty 和缓存发布的具体实现政策能降低落地差异，但不改变已明确的权威边界，因此不影响本版准确发布。
7. **验证记录分栏**：元数据明确标记“待验证”，正文也单列未验证边界，没有声称任何测试已经通过。将已完成观察和待执行验证进一步分栏只会强化证据表达，不会修正当前事实性错误，因此不影响本版准确发布。

## 发布前复算

- 复算执行者：`/root`
- 复算时间：`2026-08-26T08:58:38Z`
- 记录 SHA：`063EFD5798A41DDDBACEA8D9A8B827DCA0A11DF85EECA6A61874DA0130CCEDFB`
- 直接引用资源：无
- 与候选匹配：是
