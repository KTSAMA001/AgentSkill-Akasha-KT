# 冷读审核：Windows 受保护文件被 unix2dos 原地处理后的拒绝访问事故复盘

- 目标记录：`data/windows-esafenet-unix2dos-file-recovery.md`
- 候选 SHA-256：`071FA81E6EF02A088A043027E34F1CE5B796B82461318C6A940FEC868050168D`
- 直接引用资源：无
- 审核日期：2026-08-26
- 审核任务/上下文 ID：`/root/cold_read_incident_sanitized`
- 读者隔离：新建子 Agent，`fork_turns=none`，不继承父任务对话
- 输入白名单：目标记录、目标读者画像、审核问题；资源为无
- 目标读者：熟悉 Windows 文本文件、摘要和基本故障隔离，但不了解本次事故和内部项目的普通工程实践者
- 最终结论：可发布

## 完整审核提示

```text
你是一名高级 Windows 事故复盘读者、取证安全审查员兼无历史冷读者。通过系统提供的 Skills catalog 解析 `AgentSkill-Akasha-KT` 根目录，进入该根后，只读相对路径 `data/windows-esafenet-unix2dos-file-recovery.md`。不得在输出中打印或复述解析后的物理绝对路径。预期 SHA-256 为 `071FA81E6EF02A088A043027E34F1CE5B796B82461318C6A940FEC868050168D`。允许输入只有本提示和该目标记录；禁止读取 Git、其它 data 记录、INDEX、workflow、审核证据、对话、源码、日志、资产或外部资料，禁止修改任何文件。阅读前与形成结论前各独立计算一次目标文件 SHA-256；任一不匹配或前后变化，只能给“无法判断”。请以第一次接触事故的读者视角复述：事故触发、S0–S4 证据链、三份直接候选与两份高置信候选的区别、写回与工程验收能证明/不能证明什么、安全响应与停止条件。审核是否存在危险可复制操作、把特定版本事故外推为通用恢复法、事实/推断/未知混淆、夸大无损、跨视图摘要比较、隐私泄露、内部项目依赖或虎头蛇尾。精确软件版本和匿名测试数量只有在支撑适用边界且不能反推内部项目时才可接受。固定输出：阅读前 SHA-256；结论前 SHA-256；读者复述；敏感信息审查；发布阻断项（无则写无）；非阻断建议（无则写无）；最终结论：可发布|修改后发布|无法判断。只有零阻断且双 SHA 匹配才可发布。
```

## 实际输入与起止摘要

- 阅读前目标 SHA-256：`071FA81E6EF02A088A043027E34F1CE5B796B82461318C6A940FEC868050168D`
- 结论前目标 SHA-256：`071FA81E6EF02A088A043027E34F1CE5B796B82461318C6A940FEC868050168D`
- 阅读前资源摘要：无
- 结论前资源摘要：无

## 读者完整原始输出

阅读前 SHA-256：`071FA81E6EF02A088A043027E34F1CE5B796B82461318C6A940FEC868050168D`，与预期匹配。

结论前 SHA-256：`071FA81E6EF02A088A043027E34F1CE5B796B82461318C6A940FEC868050168D`，与预期及阅读前结果匹配，候选未变化。

读者复述：

- 事故触发：在指定的 Cobra DocGuard Client 5.2.0 与 WinLibs unix2dos 7.5.4 组合下，对受透明保护文件执行一次 `unix2dos -f` 原地处理后，目录项仍在，但普通读取、复制、移动和重命名均被拒绝。可丢弃副本稳定复现只证明该命令是直接触发因素，不证明保护驱动内部根因。
- S0：正常受保护文件，通常向普通 API 提供 C# 明文，但没有事故前可信摘要。
- S1：一次故障操作后的原目录项，普通访问被拒；它是应冻结的现场证据。
- S2：从 S1 命名导出的隔离数据，能看到保护容器字节；它是事故后副本，不是事故前备份。代表性样本等长不能外推到全部文件。
- S3：从保全的 S2 工作副本生成直接候选。三个文件的变化可由已复现的换行变化直接解释，处理后成为严格 UTF-8 C#；这仍不等于证明与事故前逐字节相同。
- S4：另外两个文件存在事故前合法 CR 的歧义，只能生成有限候选，再依赖当时实现上下文筛选。编译和目标测试支持所选内容，但筛选依据不独立、不公开，因此只能称为高置信候选，不能称为唯一正确原文。
- 三份直接候选与两份高置信候选的核心区别，是前者有已复现字节变化支撑直接逆转，后者还依赖事故上下文和工程行为筛选；两者均缺少事故前摘要，而后者的证据独立性更弱。
- 写回前后同视图明文 SHA-256 一致，只能证明候选固化、恢复包和写回没有再次改变内容。Unity 重新导入、编译、目标测试、控制台和数据不变性检查通过，只能证明当前工具链及已覆盖行为接受候选。
- 这些验证不能证明事故前后逐字节相同，也不能证明时间戳、ACL、保护状态等元数据已经恢复，不能覆盖未测试语义或解释驱动内部机制。
- 安全响应要求先冻结唯一现场、保留 S1/S2/候选与摘要，仅在可丢弃副本上复现；候选必须从保全副本写入全新排他目录。操作次数不明、版本或字节行为不一致、输出路径不安全、视图不稳定、出现非换行变化或摘要冲突时均应停止。没有独立事故前证据时，应停在候选阶段；写回还要求恢复包、明确覆盖授权和可回退条件。

敏感信息审查：

- 未发现人员身份、账号、凭据、内部路径、项目名称、源码符号、资产名称或可定位组织的信息。
- 厂商标识和精确软件版本用于限定事故适用范围，具有必要性。
- Unity 版本、匿名测试数量、回调计数及匿名修订值用于界定工程验收强度；单凭这些信息不足以反推内部项目。
- S4 仅说明依赖过事故上下文，没有披露具体类型、成员、字符串或实现内容。
- 文中标出了危险触发命令，但未给出带目标路径的可直接执行命令、恢复脚本或写回脚本，并配有明确的停写及现场保护警告。
- 未把限定版本事故外推为通用恢复法；事实、机制推断和未知项有明确分界。
- 未夸大为无损恢复；多次主动否定“同视图摘要一致”或“测试通过”等同于事故前逐字节一致。
- 跨视图摘要比较边界正确，明确禁止把容器视图与透明明文视图直接比较。
- 结尾给出了可复用证据顺序、停止条件及“高置信内容恢复”的措辞边界，不存在虎头蛇尾。

发布阻断项：无。

非阻断建议：

- 为进一步降低对驱动状态的因果暗示，可将“容器恢复到驱动可接受的状态”再明确标注为基于普通 API 重新获得明文的工作解释。
- 面向完全公开的外部发布时，可把场景修订值和回调精确次数概括为“测试前后不变”和“两个回调均被实际驱动”，进一步执行信息最小化；不影响当前结论。

最终结论：可发布

## 处理与复审

- 原未推送证据因包含本机绝对路径且绑定 CRLF 工作树摘要而失效，已从重建提交中移除。
- 本轮使用仓库相对路径和规范 Git blob 字节重新冻结候选，由新的无历史读者从头审核。
- 发布阻断项为“无”，最终结论为“可发布”；两条建议属于可选的进一步信息最小化，不改变当前记录的安全边界。

## 发布前复算

- 复算执行者：主执行者 `/root`
- 复算时间：2026-08-26T02:00:24.6880801+08:00
- 最终目标 SHA-256：`071FA81E6EF02A088A043027E34F1CE5B796B82461318C6A940FEC868050168D`
- 最终直接引用资源：无
- 与本证据输入逐项匹配：是
